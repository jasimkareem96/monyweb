# 📚 شرح أوامر Git

## 🔍 ماذا تفعل هذه الأوامر؟

### 1️⃣ `git branch -M main`

**ماذا يعني:**
- `git branch` - أمر لإدارة الفروع (branches)
- `-M` - يعني "Move/Rename" (نقل/إعادة تسمية)
- `main` - اسم الفرع الجديد

**ماذا يفعل:**
- يغير اسم الفرع الحالي إلى `main`
- إذا كان اسم الفرع `master`، سيصبح `main`
- `-M` يفرض التغيير حتى لو كان هناك فرع `main` موجود

**لماذا؟**
- GitHub يستخدم `main` كفرع افتراضي
- `main` هو المعيار الحديث (بدلاً من `master`)

---

### 2️⃣ `git push -u origin main`

**ماذا يعني:**
- `git push` - رفع الكود إلى Remote Repository
- `-u` - يعني "set upstream" (تعيين upstream)
- `origin` - اسم Remote Repository (GitHub)
- `main` - اسم الفرع المطلوب رفعه

**ماذا يفعل:**
- يرفع الكود من الفرع `main` المحلي إلى `main` على GitHub
- `-u` يربط الفرع المحلي بالفرع على GitHub
- بعد ذلك، يمكنك استخدام `git push` فقط بدون تحديد `origin main`

**بعد هذا الأمر:**
- الكود سيكون على GitHub ✅
- يمكنك رؤيته في Repository
- يمكن استيراده إلى Vercel

---

## 📋 ملخص:

```powershell
# 1. تغيير اسم الفرع إلى main
git branch -M main
# النتيجة: الفرع الآن اسمه "main"

# 2. رفع الكود إلى GitHub
git push -u origin main
# النتيجة: الكود على GitHub الآن!
```

---

## ⚠️ إذا ظهرت رسالة خطأ:

### خطأ: "remote origin already exists"
**الحل:**
```powershell
git remote remove origin
git remote add origin https://github.com/jasimkareem96/monyweb.git
```

### خطأ: "authentication failed"
**الحل:**
- استخدم GitHub Personal Access Token
- أو استخدم GitHub Desktop

### خطأ: "repository not found"
**الحل:**
- تأكد من أن Repository موجود على GitHub
- تأكد من اسم المستخدم صحيح

---

## ✅ بعد تنفيذ الأوامر:

**سترى رسالة مثل:**
```
Enumerating objects: 158, done.
Counting objects: 100% (158/158), done.
Writing objects: 100% (158/158), done.
To https://github.com/jasimkareem96/monyweb.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

**هذا يعني:** ✅ تم الرفع بنجاح!

---

## 🎯 الخطوة التالية:

**بعد الرفع:**
1. اذهب إلى GitHub
2. تحقق من أن الكود موجود
3. اذهب إلى Vercel
4. استورد المشروع

---

**هل تريد تنفيذ الأوامر الآن؟** 🚀

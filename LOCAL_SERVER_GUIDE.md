# 🖥️ تشغيل الخادم محلياً

## ✅ تم تشغيل الخادم!

**الخادم يعمل الآن في الخلفية.**

---

## 🌐 الوصول إلى المنصة:

### افتح المتصفح واذهب إلى:

```
http://localhost:3000
```

**أو:**

```
http://127.0.0.1:3000
```

---

## 🔐 معلومات تسجيل الدخول:

### 👤 Admin:
- **البريد:** `admin@monyweb.com`
- **كلمة المرور:** `123456`

### 👤 Merchant:
- **البريد:** `merchant@monyweb.com`
- **كلمة المرور:** `123456`

### 👤 Buyer:
- **البريد:** `buyer@monyweb.com`
- **كلمة المرور:** `123456`

---

## 🧪 اختبارات سريعة:

### 1. الصفحة الرئيسية:
- [ ] افتح `http://localhost:3000`
- [ ] يجب أن ترى الصفحة الرئيسية

### 2. تسجيل الدخول:
- [ ] اضغط "تسجيل الدخول"
- [ ] استخدم `admin@monyweb.com` / `123456`
- [ ] يجب أن يتم تسجيل الدخول بنجاح

### 3. لوحة التحكم:
- [ ] بعد تسجيل الدخول، اذهب إلى `/dashboard`
- [ ] يجب أن ترى لوحة التحكم

### 4. تصفح العروض:
- [ ] اذهب إلى `/offers`
- [ ] يجب أن ترى العروض المتاحة

---

## 🛑 إيقاف الخادم:

**إذا أردت إيقاف الخادم:**

```powershell
# في Terminal جديد:
taskkill /F /IM node.exe
```

**أو اضغط `Ctrl + C` في Terminal الذي يعمل فيه الخادم**

---

## 🔄 إعادة تشغيل الخادم:

```powershell
cd c:\Users\pc\Desktop\monyweb
npm run dev
```

---

## 🆘 إذا لم يعمل:

### 1. تحقق من أن الخادم يعمل:

```powershell
Get-Process -Name node
```

### 2. تحقق من المنفذ 3000:

```powershell
netstat -ano | findstr :3000
```

### 3. أعد تشغيل الخادم:

```powershell
# أوقف جميع عمليات Node
taskkill /F /IM node.exe

# أعد التشغيل
cd c:\Users\pc\Desktop\monyweb
npm run dev
```

---

## 📋 ملاحظات:

- **الخادم يعمل على:** `http://localhost:3000`
- **قاعدة البيانات:** متصلة بـ Supabase PostgreSQL
- **البيئة:** Development (محلي)

---

**جرب الآن: افتح `http://localhost:3000` في المتصفح!** 🚀
